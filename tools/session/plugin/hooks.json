{
  "//": "Narrow hook config for the Explainify session plugin. Only Stop and SessionEnd fire, and each only records a local pointer (session id + transcript path) — never the transcript content. CLAUDE_PROJECT_DIR is provided by Claude Code.",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PROJECT_DIR}/tools/session/plugin/hooks/capture-pointer.mjs\""
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PROJECT_DIR}/tools/session/plugin/hooks/capture-pointer.mjs\""
          }
        ]
      }
    ]
  }
}
